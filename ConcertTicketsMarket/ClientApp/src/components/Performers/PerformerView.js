// props for performer view means:
// type = 'c' if common (navigations), 'p' = pick - when need to pick smth.
// callback? = invoked, if type not 'c' used;

export default function PerformerView(props) {
    
}